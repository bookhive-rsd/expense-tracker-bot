services:
  - type: web
    name: expense-tracker-bot
    env: python
    plan: free
    buildCommand: |
      # Force Python 3.12 install
      apt-get update && apt-get install -y python3.12 python3.12-venv
      python3.12 -m venv .venv
      source .venv/bin/activate
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      TELEGRAM_BOT_TOKEN: 
        value: 8331852123:AAEsXzS_0riHRuDI3P-caN3gLtRV6colVjo  # Revoke old one via @BotFather!
      MONGO_URI: 
        value: mongodb+srv://bookhiversd:8D6pLujBM9rLVi8B@bookhive.7h76ryz.mongodb.net/DailyExpenseTrackerApp?retryWrites=true&w=majority&appName=BookHive
